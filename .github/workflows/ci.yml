name: ci

# Pure-Go TUI ; no kernel / netlink / hypervisor deps. CI just
# vets, tests with the race detector, and cross-builds for the
# operator's two reference platforms (Linux arm64 for the 3-DC
# debian fleet ; Darwin arm64 for the Mac laptop). Push + PR
# only — no publishing, no tag gating.
#
# go.mod carries `replace github.com/openweft/weft-proto =>
# ../weft-proto` for the sibling-dev workflow. Every job checks
# the proto repo out next to weft-tui so the replace resolves.

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    name: vet + test (race)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          path: weft-tui

      - uses: actions/checkout@v7
        with:
          repository: openweft/weft-proto
          path: weft-proto

      - uses: actions/setup-go@v6
        with:
          go-version-file: weft-tui/go.mod
          cache: true
          cache-dependency-path: weft-tui/go.sum

      - name: gofmt check
        working-directory: weft-tui
        run: |
          out=$(gofmt -l .)
          if [ -n "$out" ]; then
            echo "::error::gofmt drift in :"
            echo "$out"
            exit 1
          fi

      - name: go vet
        working-directory: weft-tui
        run: go vet ./...

      - name: go test -race
        working-directory: weft-tui
        run: go test -race -count=1 -timeout 5m ./...

  cross-build:
    name: cross-build ${{ matrix.goos }}/${{ matrix.goarch }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - { goos: linux,  goarch: arm64 }   # 3-DC reference
          - { goos: linux,  goarch: amd64 }
          - { goos: darwin, goarch: arm64 }   # operator laptop
    steps:
      - uses: actions/checkout@v7
        with:
          path: weft-tui

      - uses: actions/checkout@v7
        with:
          repository: openweft/weft-proto
          path: weft-proto

      - uses: actions/setup-go@v6
        with:
          go-version-file: weft-tui/go.mod
          cache: true
          cache-dependency-path: weft-tui/go.sum

      - name: build
        working-directory: weft-tui
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: "0"
        run: go build -trimpath -ldflags="-s -w" -o /tmp/weft-tui-${{ matrix.goos }}-${{ matrix.goarch }} .
